# Runtime deps. Core pipeline (index/reconcile/render) is stdlib-only.
anthropic>=0.40  # only needed for the LLM plan step (make_plan)
pygments>=2.17   # optional: syntax highlighting in the rendered diffs; degrades gracefully
